{
  "name": "nickname-gym-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check server.mjs && node --check public/app.js && node --check scripts/smoke-test.mjs",
    "start": "node server.mjs",
    "test": "node scripts/smoke-test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
